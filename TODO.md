# 📋 TODO — канбан задач

> **Статусы = секции ниже, сверху вниз по приоритету:**
> 🟢 **В РАБОТЕ** — решено, делаем; порядок = очередь (верх = следующая). Хук гонит брать следующую `[ ]`. Не «одна задача» — независимые можно параллелить.
> 🟧 **ОЖИДАЕТ РЕШЕНИЯ ЗАКАЗЧИКА** — мяч на стороне Заказчика: нужен ответ/выбор, прежде чем продолжить. Активна, не «потом».
> 🟣 **ПОЗЖЕ / ОТЛОЖЕНО** — сформулирована (есть «Готово ="), но припаркована (не сейчас). Хук НЕ гонит.
> 🧊 **ИДЕИ** — абстрактные, ещё не проработаны, без «Готово =».
> ✅ Готово → вычеркнуть отсюда и записать в ЖУРНАЛ (Историю решений). Здесь готовое НЕ копим.
>
> Принцип «В РАБОТЕ vs ПОЗЖЕ» = «можно брать прямо сейчас?» (да → В РАБОТЕ; припаркована → ПОЗЖЕ).
> Маркеры для хука: `[ ]` не начата · `[~]` в работе · `[x]` готова · `[?]` ждёт решения Заказчика.
> Нужно решение Заказчика → `[?]` + ❓ + перенеси в «ОЖИДАЕТ РЕШЕНИЯ ЗАКАЗЧИКА».

---

## 🟢 В РАБОТЕ

_(пусто — верхняя задача ждёт выбора темы Заказчиком, см. «ОЖИДАЕТ РЕШЕНИЯ»)_

---

## 🟧 ОЖИДАЕТ РЕШЕНИЯ ЗАКАЗЧИКА

- [?] Прогнать новую тему через functional_v2 и сравнить с Кофейней v1 ❓
      Готово = реальный прогон одной темы (любой кроме Кофейни) на функционально-v2 режиме, сравнение шортлиста с Кофейней v1 записано в Журнал → видно даёт ли v2 сдвиг.

      ❓ Решение Заказчика: какую тему гнать. Ориентир — эстетика/ритуал/идентичность (где у v1 ожидается потолок), не операционная боль. Кофейня = эталон сравнения, не повторять. Вернуть в «В РАБОТЕ» когда тема выбрана.

- [?] Удаление карточки с child-ами: каскад или запрет? ❓
      ТЗ говорит — спроси Заказчика. Варианты: (а) запретить с пояснением «есть child-карточки», (б) удалять каскадно с подтверждением «удалит N child-карточек». Решение нужно перед тем как делать UI удаления.

---

## 🟣 ПОЗЖЕ / ОТЛОЖЕНО

- [ ] Раскатать обновлённый kit на Nokki / Scandiwall / OpenClaw
      Контекст: эталон-архив `claude-kit-starter.zip` и Цербро обновлены (compound разрешён, guard-subagent ретайрнут, провижининг в АРХИТЕКТУРЕ, УСТАНОВКА удалён, mobile=Playwright, моб-нудж, фикс упавшего скрина, канбан-TODO). Остальные проекты ещё на старом ките.

      Готово = в каждом проекте обновлены generic-файлы (CLAUDE.md, guard-bash.js, guard-subagent.js, visual-check.js, todo-drain.js, «ХУКИ И СКРИПТЫ - АРХИТЕКТУРА.md», тесты, TODO-шаблон), удалён УСТАНОВКА.md; project-специфичные ПРОЕКТ/ЖУРНАЛ/TODO/guard-files НЕ трогать. Делать в сессии каждого проекта.

- [ ] External critic POC для 4.3 (отдельным вызовом, не self-check)
      Готово = функция в openai.ts, кнопка на карточке гипотезы, нейтрально-калиброванный промпт (распределение 1-10 баллов, без «режь беспощадно»), результат пишется в карточку или отдельную секцию.

---

## 🧊 ИДЕИ — когда-нибудь (без «Готово =»)

- Креативный режим (creative methodology mode) — третий пресет после functional_v1/v2. Включить только если v2 даст потолок (нет сдвига или сдвиг недостаточный).
- Кросс-сравнение шортлистов разных режимов на одной теме как фича UI (а не вручную в Журнале).
- Авто-расчёт стоимости прогона по тарифам моделей перед запуском auto-all (сейчас оценка вручную $1.0-1.5).
- **Карточки-ветки со своими переписками** (канбан-приложение): per-thread разговор по каждому багу/идее + статусы. Отдельный продукт; частично закрывается «сессия = ветка» + этот TODO как индекс.
