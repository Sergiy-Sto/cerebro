# 📋 TODO — очередь задач

> Формат: каждая задача — со строкой «Готово =» (критерий приёмки, проверяемый без Заказчика, если возможно).
> Порядок = сверху вниз, очередность НЕ переспрашивать. Закрыл задачу → бери следующую `[ ]` (контролирует Stop-hook).
> Нужно решение Заказчика → пометь `[?]`, перенеси в «Заблокировано», задай вопрос с ❓.
>
> 🟢 сейчас / в работе («зелёный свет») · 🟧 позже · ⛔ заблокирована или отменена · ✅ готово · ❓🟢 в работе, но зависла — нужно внимание Заказчика
> Маркеры для хука/статуса: `[ ]` не начата · `[~]` в работе · `[x]` готово · `[?]` заблокирована

## Сейчас
_(пусто — верхняя задача ждёт выбора темы Заказчиком, см. «Заблокировано»)_

## Дальше
- [ ] 🟧 Рабочий мобильный скриншот для kit (Rule 2 visual-check, desktop+mobile)
      Готово = снят реальный скрин app в мобильном вьюпорте (≤390px, виден reflow в одну колонку). Выбор (2026-06-15): **Playwright MCP — основной путь для кита** (ставится глобально один раз → все проекты, любой URL локал+прод, реальная эмуляция). 👉 Требует разового подключения в Desktop (Connectors/Extensions) — действие Заказчика. Google-логин НЕ нужен (наш app публичный). Запасные: `Claude_Preview` (0 установки, но только локальный dev-сервер), либо `npm i -D playwright` + `scripts/mobile-shot.sh <url>` (per-project, durable/CI). Chrome MCP resize НЕ годится (проверено). Решение → строка в инвентарь CLAUDE.md.
- [ ] 🟧 External critic POC для 4.3 (отдельным вызовом, не self-check)
      Готово = функция в openai.ts, кнопка на карточке гипотезы, нейтрально-калиброванный промпт (распределение 1-10 баллов, без «режь беспощадно»), результат пишется в карточку или отдельную секцию.

## Заблокировано (ждёт решения Заказчика)
- [?] 🟢 Прогнать новую тему через functional_v2 и сравнить с Кофейней v1 ❓ (отложено Заказчиком на завтра — нужен выбор темы)
      Готово = реальный прогон одной темы (любой кроме Кофейни) на функционально-v2 режиме, сравнение шортлиста с Кофейней v1 записано в Журнал → видно даёт ли v2 сдвиг.
      Решение Заказчика: какую тему гнать. Ориентир — эстетика/ритуал/идентичность (где у v1 ожидается потолок), не операционная боль. Кофейня = эталон сравнения, не повторять. Вернуть в «Сейчас» когда тема выбрана.

- [?] ⛔ Удаление карточки с child-ами: каскад или запрет? ❓
      ТЗ говорит — спроси Заказчика. Варианты: (а) запретить с пояснением «есть child-карточки», (б) удалять каскадно с подтверждением «удалит N child-карточек». Решение нужно перед тем как делать UI удаления.

## 🧊 Идеи / когда-нибудь (не очередь, без «готово =»)
- Креативный режим (creative methodology mode) — третий пресет после functional_v1/v2. Включить только если v2 даст потолок (нет сдвига или сдвиг недостаточный).
- Кросс-сравнение шортлистов разных режимов на одной теме как фича UI (а не вручную в Журнале).
- Авто-расчёт стоимости прогона по тарифам моделей перед запуском auto-all (сейчас оценка вручную $1.0-1.5).
