# 📋 TODO — канбан задач

_(справка по статусам и формату — в конце файла, сворачивается)_

---

## 🟢 В РАБОТЕ

_(пусто — верхняя задача ждёт выбора темы Заказчиком, см. «ОЖИДАЕТ РЕШЕНИЯ»)_

---

## 🟧 ОЖИДАЕТ РЕШЕНИЯ ЗАКАЗЧИКА

- [?]🟧 **Прогнать новую тему через functional_v2 и сравнить с Кофейней v1**
      Описание: проверить, даёт ли v2 реальный сдвиг шортлиста vs v1 — прогнать одну тему (не Кофейню) на v2-режиме, сравнение записать в Журнал.
      Готово = реальный прогон + сравнение шортлиста с Кофейней v1 в Журнале → видно, даёт ли v2 сдвиг.
      ❓ Решение Заказчика: какую тему гнать. Ориентир — эстетика / ритуал / идентичность (где у v1 ожидается потолок), не операционная боль. Кофейня = эталон сравнения, не повторять. Вернуть в «🟢 В РАБОТЕ» когда тема выбрана.

---

- [?]🟧 **Удаление карточки с child-ами: каскад или запрет?**
      Описание: перед реализацией UI удаления нужно решить поведение для карточек, у которых есть дочерние.
      Готово = выбран и зафиксирован вариант поведения → можно делать UI удаления.
      ❓ Решение Заказчика: (а) запретить с пояснением «есть child-карточки»; (б) удалять каскадно с подтверждением «удалит N child-карточек».

---

## 🟣 ПОЗЖЕ / ОТЛОЖЕНО

- [ ]🟣 **Раскатать обновлённый kit на Nokki / Scandiwall / OpenClaw**
      Описание: эталон-архив `claude-kit-starter.zip` и Цербро обновлены (compound разрешён, провижининг, моб-нудж, фикс упавшего скрина, канбан-TODO, фикс todo-drain). Остальные проекты ещё на старом ките.
      На карандаше: обновить generic-файлы (CLAUDE.md, guard-bash.js, guard-subagent.js, visual-check.js, todo-drain.js, «ХУКИ И СКРИПТЫ - АРХИТЕКТУРА.md», тесты, TODO-шаблон), удалить УСТАНОВКА.md. НЕ трогать project-специфичные ПРОЕКТ/ЖУРНАЛ/TODO/guard-files. Делать в сессии каждого проекта.
      Готово = в каждом проекте generic-файлы обновлены, УСТАНОВКА.md удалён.

---

- [ ]🟣 **External critic POC для 4.3** (отдельным вызовом, не self-check)
      Описание: внешний критик гипотез 4.3 отдельным API-вызовом, а не self-check внутри генерации.
      На карандаше: промпт нейтрально-калиброванный (распределение 1-10 баллов, без «режь беспощадно»).
      Готово = функция в openai.ts + кнопка на карточке гипотезы + результат пишется в карточку или отдельную секцию.

---

## 🧊 ИДЕИ — когда-нибудь (без «Готово =»)

- Креативный режим (creative methodology mode) — третий пресет после functional_v1/v2. Включить только если v2 даст потолок (нет сдвига или сдвиг недостаточный).
- Кросс-сравнение шортлистов разных режимов на одной теме как фича UI (а не вручную в Журнале).
- Авто-расчёт стоимости прогона по тарифам моделей перед запуском auto-all (сейчас оценка вручную $1.0-1.5).
- **Карточки-ветки со своими переписками** (канбан-приложение): per-thread разговор по каждому багу/идее + статусы. Отдельный продукт; частично закрывается «сессия = ветка» + этот TODO как индекс.

---

## ℹ️ Как вести этот файл (справка — сворачивается)

> **Статусы = секции выше, сверху вниз по приоритету:**
> 🟢 **В РАБОТЕ** — решено, делаем; порядок = очередь (верх = следующая). Хук todo-drain гонит брать следующую `[ ]`. Не «одна задача» — независимые можно параллелить. Наполняет Claude (очевидное/безопасное сам; крупное/рискованное — сначала спрашивает).
> 🟧 **ОЖИДАЕТ РЕШЕНИЯ ЗАКАЗЧИКА** — мяч на стороне Заказчика: нужен ответ/выбор. Активна, не «потом».
> 🟣 **ПОЗЖЕ / ОТЛОЖЕНО** — сформулирована (есть «Готово =»), но припаркована. Хук НЕ гонит.
> 🧊 **ИДЕИ** — абстрактные, ещё не проработаны, без «Готово =».
> ✅ Готово → вычеркнуть отсюда и записать в ЖУРНАЛ (Историю решений). Здесь готовое НЕ копим.
>
> **Формат карточки:** `- [маркер]🟢/🟧/🟣 **Заголовок**` — эмодзи статуса В НАЧАЛЕ строки + «Описание: что/зачем» + «На карандаше: нюансы» (если есть) + «Готово = критерий». ❓ ставится ТОЛЬКО на строке самого вопроса к Заказчику. Карточки разделяй полосой `---`.
> Принцип «В РАБОТЕ vs ПОЗЖЕ» = «можно брать прямо сейчас?» (да → В РАБОТЕ; припаркована → ПОЗЖЕ).
> Маркеры для хука: `[ ]` не начата · `[~]` в работе · `[x]` готова · `[?]` ждёт решения Заказчика.
