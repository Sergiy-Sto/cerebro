import type { StageId, Project, Card } from '../state/types';

function formatContext(project: Project): string {
  const parts = [`ТЕМА (держи в фокусе на протяжении всего ответа): ${project.frame}`];
  if (project.constraints.length > 0)
    parts.push(`Ограничения: ${project.constraints.join('; ')}`);
  if (project.criteria.length > 0)
    parts.push(`Критерии успеха: ${project.criteria.join('; ')}`);
  return parts.join('\n');
}

function formatCards(cards: Card[]): string {
  return cards.map((c) => {
    const star = c.status === 'interesting' ? '★ ' : '';
    return `- [id:${c.id}] ${star}${c.title}: ${c.description}`;
  }).join('\n');
}

function existingBlock(existing: string): string {
  if (!existing) return '';
  return `\nCRITICAL — these cards already exist. You MUST NOT repeat, rephrase, or generate anything thematically similar to them:\n${existing}\n`;
}

// ─────────────────────────────────────────────────────────────
// Общие правила формата (приклеиваются ко всем промптам)
// ─────────────────────────────────────────────────────────────

const FMT_RULES = `ПРАВИЛА ФОРМАТА КАРТОЧЕК:

1. **Маркеры уверенности per-пункт.** Каждый пункт в перечислениях помечай в квадратных скобках:
   - [факт] — общеизвестный факт, который можно подтвердить документально
   - [типовой паттерн] — устоявшаяся практика, наблюдаемая в реальном мире
   - [предположение] — твоя оценка, требующая проверки
   - [нужно проверить] — гипотеза, в которой ты не уверен — для Search Scan

2. **Богатство = разнообразие углов зрения, не количество пунктов.**
   - Дай столько пунктов, сколько содержательно оправдано
   - НЕ натягивай искусственно до круглого числа
   - Если по какому-то углу нечего сказать — пропусти его, не выдумывай
   - Если пунктов оказалось меньше 5 — это нормально, объясни почему так

3. **Каждый пункт = название + 2-3 содержательных предложения.** Не одна фраза. Контекст, специфика, нюансы.

4. **Missing Angles Check.** После основного списка добавь 1-2 строчки:
   "Возможно упустил: [нишевый случай] / [региональная специфика] / [устаревший вариант] / [B2B-сегмент] — стоит проверить."

5. **Формат перечисления:** маркированный список через переносы строк (\\n):
   "Вступительная фраза:
   • Название [маркер] — раскрытие 2-3 предложениями
   • Название [маркер] — раскрытие
   ..."

6. **Линейная мысль** (аналитический вывод, не перечисление) — развёрнутыми абзацами с конкретикой.

7. **Запреты:**
   - НЕ генерируй идеи / "а что если" / гипотезы — это следующие модули
   - НЕ предлагай продукты, MVP, бизнес-решения
   - НЕ пиши эссе или обзор
   - Каждый пункт должен быть пригоден как вход для следующего модуля

8. **Язык — только русский.** Никакого английского кроме терминов/имён собственных.

9. **Простой язык.** Пиши так, чтобы понял человек НЕ из этой отрасли.
   - Объясняй любой специфический термин в скобках при первом упоминании: "эскроу (счёт, на котором деньги замораживаются до выполнения условий)"
   - Не используй жаргон ради звучности
   - Если идея сложная — раскрой её через бытовой пример
   - Заумная карточка = неработающая карточка: на ней не получится строить следующие этапы методологии

10. **Переносы строк в JSON:** оформляй как \\n внутри строки описания. Сохраняй валидный JSON.

ФОРМАТ ВЫВОДА:
Каждая карточка = отдельный JSON-объект на своей строке.
{"title": "...", "description": "...", "tags": ["..."], "derived_from": ["id1", "id2"]}

Поле derived_from — массив ID карточек из контекста выше, на основе которых построена эта новая карточка.`;

const FMT_RULES_METRICS = `${FMT_RULES}

ДОПОЛНИТЕЛЬНО: для каждой карточки укажи метрики и краткий анализ:
{"title": "...", "description": "...", "tags": ["..."], "derived_from": ["id1", "id2"], "metrics": {"novelty": 8, "strength": 7, "feasibility": 6, "testability": 9}, "analysis": "Новизна 8: [почему неочевидно]. Сила 7: [масштаб возможности]. Реализация 6: [что сложно]. Проверка 9: [как быстро протестировать]."}

SELF-CHECK перед каждой идеей:
1. Если бы ты вбил эту идею в Google — она оказалась бы в топ-10 результатов? Если да — выброси и думай глубже.
2. Оцени Новизну от 1 до 10. Если < 7 — замени идею на более неожиданную, затем оценивай снова.`;

type PromptFn = (ctx: string, prev: string, existing: string) => string;

// ─────────────────────────────────────────────────────────────
// Module 01: Entity / Reality Mapping
// ─────────────────────────────────────────────────────────────

const STAGE_PROMPTS: Record<StageId, PromptFn> = {

  // ═════ 1.1 Observation Scan ═════
  observation_scan: (ctx, _prev, existing) => `${ctx}
${existingBlock(existing)}
Stage: 1.1 OBSERVATION SCAN

Цель: построить первичную карту реальности вокруг темы — только из твоих знаний, без поиска. То что ты УЖЕ знаешь о теме как явлении в мире.

ЗАПРЕТЫ (важно): не генерируй идеи, не предлагай продукты, не делай "а что если", не пиши эссе. Только карта того что есть.

Сгенерируй карточки по релевантным линзам (выбери те что осмысленны для этой темы):

ЛИНЗЫ:
1. ТИПЫ И РАЗНОВИДНОСТИ — категории, подвиды, классификации
2. РЕАЛЬНЫЕ СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ — когда и зачем люди с этим сталкиваются
3. СОВРЕМЕННЫЕ ФОРМАТЫ — как это выглядит сейчас
4. УСТАРЕВШИЕ ФОРМАТЫ — что отмирает, ушло в историю
5. СМЕЖНЫЕ СУЩНОСТИ — что рядом, часто рассматривается вместе
6. ТИПИЧНЫЕ ПРОБЛЕМЫ — известные жалобы, провалы, болевые точки
7. ТИПИЧНЫЕ КОНТЕКСТЫ — в каких ситуациях возникает потребность
8. ЧТО СТОИТ ПРОВЕРИТЬ ПОИСКОМ — что я могу не знать или мои данные могут устареть

В КАЖДОЙ ЛИНЗЕ копни глубже через структурные оси (если релевантно теме):
- по глубине / масштабу (поверхностно ↔ радикально)
- по цели (необходимость ↔ улучшение ↔ статус)
- по бюджету / классу (массовый ↔ премиум)
- по региону (локально ↔ глобально, отличия в странах)
- по аудитории (B2C / B2B / профи / DIY)
- по времени (вчера ↔ сейчас ↔ тренд)

Одна карточка = одна линза. Если по линзе нечего сказать — пропусти её, не выдумывай.

${FMT_RULES}`,

  // ═════ 1.2 Search Scan (через web search) ═════
  search_scan: (ctx, prev, existing) => `${ctx}
${prev ? `\nObservation Scan (используй как основу — derived_from):\n${prev}` : ''}
${existingBlock(existing)}
Stage: 1.2 SEARCH SCAN

Цель: проверить, расширить и обогатить Observation Scan через РЕАЛЬНЫЙ поиск в интернете.

ТЫ ИМЕЕШЬ ДОСТУП К ВЕБ-ПОИСКУ (web_search tool). Используй его агрессивно.

ПРАВИЛА ПОИСКА:
- **Максимум 12 поисковых запросов на эту сессию.**
- **Обязательные линзы (по каждой минимум 1-2 поиска):**
  • СУЩЕСТВУЮЩИЕ РЕШЕНИЯ — какие приложения, сервисы, продукты уже работают в этой нише
  • РЕАЛЬНЫЕ ПРОБЛЕМЫ ПОЛЬЗОВАТЕЛЕЙ — что в Reddit / форумах / отзывах
  • ЗАМЕНИТЕЛИ — что используют вместо основного решения
- **Опциональные линзы (если релевантны теме):**
  • Региональные особенности (US/EU/Asia сравнение)
  • Свежие тренды и нововведения
  • Failure stories и горький опыт
  • Нишевые и необычные варианты
  • Цифры, статистика, размер рынка

ЗАПРОСЫ — НА АНГЛИЙСКОМ (лучший охват). Конкретные, не общие:
- ❌ "renovation"
- ✅ "renovation budget overrun reddit 2026"
- ✅ "best renovation planning apps comparison"

КАЖДЫЙ ПОИСК должен принести КОНКРЕТНЫЙ ФАКТ / ЦИФРУ / ИМЯ / ЦИТАТУ. Если поиск дал пустоту — попробуй переформулировать.

ВЫВОД: одна карточка = одна линза. В description:
- Что нашлось (с конкретикой)
- Пометка confidence для каждого пункта: [факт] / [типовой паттерн] / [предположение] / [нужно проверить]
- Ссылки на источники (URL) рядом с фактами

Если по какой-то линзе свежей информации не нашлось — явно укажи "по этой линзе свежей информации в поиске не обнаружено — пробел для дальнейшего ручного исследования".

${FMT_RULES}`,

  // ═════ 1.3 Fundamental Dimensions ═════
  fundamental_dimensions: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping контекст (используй как основу — derived_from):\n${prev}` : ''}
${existingBlock(existing)}
Stage: 1.3 FUNDAMENTAL DIMENSIONS

Цель: разложить сущность на фундаментальные измерения. Это аналитический рентген сущности.

ЗАПРЕТЫ: не идеи, не "а что если", не предложения. Только разложение.

СНАЧАЛА (молча, без карточки): определи тип сущности —
A) физический объект | B) процесс / деятельность | C) система / сервис | D) роль / профессия | E) информационный объект | F) опыт / состояние | G) смешанная

Если смешанная — явно перечисли её компоненты в одной из первых карточек.

ИЗМЕРЕНИЯ (выбери релевантные для типа сущности):

ДЛЯ ОБЪЕКТА: назначение, функции, материалы, форма, конструкция, опоры, размеры, взаимодействие с телом, среда использования, жизненный цикл, ограничения.

ДЛЯ ПРОЦЕССА: назначение, ожидаемый результат, ресурсы, этапы, роли, зависимости, интерфейсы, среда, временная логика, ограничения, точки неопределённости.

ДЛЯ СЕРВИСА: точка контакта, каналы взаимодействия, value flow, точки оплаты, retention loops, операционные процессы.

Одна карточка = одно измерение. Для каждого:
- название измерения
- описание (что это значит для данной сущности)
- конкретные элементы внутри измерения
- почему это важно для дальнейшего анализа

${FMT_RULES}`,

  // ═════ 1.4 Obligatory vs Accidental Features ═════
  obligatory_vs_accidental: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping контекст (особенно Fundamental Dimensions):\n${prev}` : ''}
${existingBlock(existing)}
Stage: 1.4 OBLIGATORY vs ACCIDENTAL FEATURES

Цель: разделить признаки сущности на обязательные и случайные. Это критический мост к Feature Challenge (Module 02) — именно случайные признаки потом подвергаются трансформациям.

ЗАПРЕТЫ: НЕ задавай "а что если убрать?" — это следующий модуль. Здесь только классификация.

ДВЕ ГРУППЫ:

**OBLIGATORY FEATURES** — без них сущность перестаёт быть собой
- Это сущностное ядро. Удали — будет другая сущность.
- Примеры для стола: горизонтальная поверхность, поднята на опоры, доступна человеку.

**ACCIDENTAL FEATURES** — часто встречается, но не обязательно
- Это историческая конвенция, культурное наследие, технологическое ограничение прошлого.
- Примеры для стола: 4 ножки, дерево, прямоугольная форма, стоит на полу, статичная высота.
- ИМЕННО ЗДЕСЬ потенциал для трансформаций.

Карточки делай раздельно: одна карточка = один признак.
- title: "Обязательное / Случайное: <признак>"
- description:
  • признак чётко сформулирован
  • почему обязательный/случайный (аргументация)
  • пример где этот признак встречается
  • для случайных — короткий намёк "а исторически почему так сложилось" (без перехода к "а что если")

Стремись к **балансу** — по 5-8 обязательных и 7-12 случайных. Случайных обычно больше — это нормально.

${FMT_RULES}`,

  // ═════ 1.5 Jobs-to-be-done ═════
  jobs_to_be_done: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping контекст:\n${prev}` : ''}
${existingBlock(existing)}
Stage: 1.5 JOBS-TO-BE-DONE

Цель: определить какую "работу" сущность выполняет в жизни человека/бизнеса/системы.

**КРИТИЧЕСКОЕ РАЗЛИЧИЕ:**
- ❌ Функция (что делает технически): "стол удерживает предметы на поверхности"
- ✅ JTBD (какую жизненную задачу решает): "даёт место для еды, работы, общения, ритуала, статуса"

ТИПЫ JOBS:
- **Практические** — конкретная утилитарная задача
- **Эмоциональные** — что чувствует человек, когда задача решена
- **Социальные** — как воспринимают окружающие
- **Идентичностные** — кем становится / каким видит себя человек

Одна карточка = один job. Для каждого:
- title: "JTBD: <короткая формулировка работы>"
- description:
  • работа (что именно "нанимают" сущность сделать)
  • для кого это важно (конкретный сегмент/контекст)
  • зачем (какая глубинная мотивация)
  • какой результат человек хочет (success criteria по его собственному ощущению)
  • в каком контексте этот job становится острым (триггер)

ЗАПРЕТЫ: не путай с features/функциями. Не превращай в продуктовые гипотезы.

${FMT_RULES}`,

  // ═════ 1.6 Users / Actors ═════
  users_actors: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping контекст (особенно Jobs-to-be-done):\n${prev}` : ''}
${existingBlock(existing)}
Stage: 1.6 USERS / ACTORS

Цель: определить всех участников сущности и их интересы. Это карта ролей и конфликтов.

ВКЛЮЧАЙ ВСЕХ:
- основной пользователь / покупатель
- исполнитель / поставщик услуги
- посредник / агрегатор
- регулятор / контролёр
- финансовый стейкхолдер (платит, но не пользуется)
- косвенно пострадавшая сторона (сосед, инспектор)
- конкурент / альтернатива

Одна карточка = один actor. Для каждого:
- title: "<Роль>: <короткая характеристика>"
- description:
  • кто это (роль и контекст)
  • что хочет (главная цель в процессе)
  • чего боится (главный страх / болезненная точка)
  • чем измеряет успех (его собственные KPI)
  • где конфликтует с другими ролями
  • какую власть имеет (что может заблокировать, отозвать, ускорить)

ВАЖНО: одна и та же физическая персона может играть разные роли в разных контекстах — выделяй роли, не людей.

ЗАПРЕТЫ: не описывай "идеального клиента" в маркетинговых терминах. Только реальных участников с их конфликтами.

${FMT_RULES}`,

  // ═════ 1.7 Failure Modes ═════
  failure_modes: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping контекст:\n${prev}` : ''}
${existingBlock(existing)}
Stage: 1.7 FAILURE MODES

Цель: найти где сущность ломается, раздражает, дорожает, затягивается, даёт плохой результат. Это карта боли — позже из неё растут гипотезы.

ИСКЛЮЧЕНИЕ: НЕ путай с Substitutes (это следующий блок). Failure modes = где сущность сама по себе плохо работает. Substitutes = чем её заменяют.

ИСКАТЬ:
- где возникают ошибки и сбои
- где люди тратят слишком много времени / денег / нервов
- где результат не совпадает с ожиданием
- где появляются конфликты между actors (из 1.6)
- где случаются скрытые расходы, "невидимый налог"
- где люди боятся, тревожатся, прокрастинируют
- где появляется хаос, путаница, потеря контроля
- где люди придумывают костыли и обходы

Одна карточка = один failure mode. Для каждого:
- title: "Сбой: <короткая формулировка>"
- description:
  • что именно ломается (конкретный сценарий)
  • для кого это проблема (какой actor из 1.6 страдает)
  • почему возникает (корневая причина или несколько)
  • как часто (всегда / иногда / только в определённых условиях)
  • последствия (что разрушается, теряется, ухудшается)
  • как сейчас люди справляются / обходят / страдают

ЗАПРЕТЫ: не предлагай решения, не пиши "а вот если бы...". Только диагностика.

${FMT_RULES}`,

  // ═════ 1.8 Substitutes ═════
  substitutes: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping контекст (особенно Failure Modes и JTBD):\n${prev}` : ''}
${existingBlock(existing)}
Stage: 1.8 SUBSTITUTES

Цель: понять чем люди заменяют сущность когда не хотят или не могут использовать её напрямую. Это очень сильный блок — иногда самая мощная идея рождается не как "лучшая X", а как "замена X".

ИСКАТЬ:
- что используют ВМЕСТО (прямые альтернативы)
- что используют ОБХОДЯ (creative workarounds)
- что используют ЧАСТИЧНО (заменяет 30-70% функции)
- "ничего не делать" как тоже валидная замена
- временные / разовые / стихийные решения

Одна карточка = один substitute. Для каждого:
- title: "Замена: <что используется вместо>"
- description:
  • что это (конкретно)
  • что именно заменяет (какую часть основной сущности)
  • почему человек выбирает замену (мотив: цена / скорость / простота / страх / отказ)
  • что выигрывает при замене (плюс)
  • что теряет при замене (минус — обычно качество, контроль, надёжность)
  • в каком сегменте/контексте замена особенно популярна

ЗАПРЕТЫ: не оценивай замены как "хорошие" или "плохие" — описывай объективно.

${FMT_RULES}`,

  // ═════ 1.9 Boundary Cases ═════
  boundary_cases: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping контекст:\n${prev}` : ''}
${existingBlock(existing)}
Stage: 1.9 BOUNDARY CASES

Цель: найти пограничные случаи — что почти является этой сущностью, но не совсем. Это расширение поля, помогает не мыслить узко.

ИСКАТЬ:
- что выглядит как сущность, но юридически/функционально не является
- что является сущностью только частично / в определённых условиях
- где понятие размывается / спорно
- крайние формы, доведённые до абсурда
- гибриды с соседними сущностями
- технологические нововведения, расширяющие классическое определение

Одна карточка = один boundary case. Для каждого:
- title: "Граница: <случай>"
- description:
  • что это (конкретно)
  • почему это ПОЧТИ относится к основной сущности
  • почему это НЕ СОВСЕМ она
  • что этот случай добавляет к пониманию сущности (расширяет какую сторону)
  • потенциальная точка для нового формата / нового продукта

ЗАПРЕТЫ: не превращай в продуктовые идеи. Только классификация и наблюдение.

${FMT_RULES}`,

  // ═════ Module 02: Feature Challenge ═════
  feature_challenge: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping контекст (особенно Obligatory vs Accidental):\n${prev}` : ''}
${existingBlock(existing)}
Stage: 2.1 FEATURE CHALLENGE — VARIANTS (атака на СЛУЧАЙНЫЕ признаки)

Цель: породить **варианты сущности** через серию трансформаций над её случайными (accidental) признаками. Здесь начинается креатив, но мы остаёмся в той же категории — это **разновидности** той же сущности, не новые сущности.

Возьми из 1.4 (Obligatory vs Accidental) ИМЕННО СЛУЧАЙНЫЕ признаки (то что историческая конвенция, а не суть). Каждому брось вызов:
- Что если УБРАТЬ это?
- Что если ИНВЕРТИРОВАТЬ (сделать наоборот)?
- Что если ЗАМЕНИТЬ на противоположное?
- Что если АВТОМАТИЗИРОВАТЬ?
- Что если ПЕРЕНЕСТИ в цифровую форму?
- Что если СДЕЛАТЬ ДО основного процесса?
- Что если СДЕЛАТЬ ПОСЛЕ?
- Что если ОТДАТЬ другой роли?
- Что если СДЕЛАТЬ ДЕШЕВЛЕ / БЫСТРЕЕ / ПРОЩЕ / ПРЕМИАЛЬНЕЕ?
- Что если СОВМЕСТИТЬ с другой сущностью?
- Что если СДЕЛАТЬ САМОВОССТАНАВЛИВАЮЩИМСЯ?

Каждая карточка = один сильный challenge. Сгенерируй 6-10.

ПРАВИЛА:
- Работаем ТОЛЬКО с accidental признаками. Радикальное переосмысление obligatory — в следующем под-модуле 2.2 Obligatory Reframing.
- Не выбирай очевидные трансформации. Если challenge звучит как известный продукт — иди глубже.
- Хороший challenge вызывает мысль "стоп, а так разве можно?" — это сигнал что попал.
- Результат — это вариант сущности (стол на одной ножке = всё ещё стол), не новая категория.
- Title формат: "Variant: <тип трансформации> <что именно меняем>"
- derived_from: ID карточек из 1.4 (именно Accidental Features)

${FMT_RULES}`,

  obligatory_reframing: (ctx, prev, existing) => `${ctx}
${prev ? `\nReality Mapping + Feature Challenge контекст (особенно 1.4 Obligatory Features и 2.1 Variants):\n${prev}` : ''}
${existingBlock(existing)}
Stage: 2.2 OBLIGATORY REFRAMING — RADICAL (атака на ОБЯЗАТЕЛЬНЫЕ признаки)

Цель: породить **категориально новые** ответы на ту же job-to-be-done через радикальное переопределение сущности. Здесь мы **выходим за пределы** сущности — получаем не варианты её, а заменители / новые категории.

Это самый творческий этап. Именно так появились iPad (сломал "клавиатуру у компьютера"), Tesla ("двигатель внутреннего сгорания"), Uber ("владение машиной таксистом"), Airbnb ("гостиница как бизнес-сущность"), Spotify ("владение музыкальным носителем"), Netflix ("прокат на физическом носителе").

Возьми из 1.4 ИМЕННО ОБЯЗАТЕЛЬНЫЕ признаки (без которых сущность как будто перестаёт быть собой). Для каждого задай:
- Что если этого признака НЕ БУДЕТ? Что станет нужно вместо?
- Какую job-to-be-done сущность реально выполняла (см. 1.5 JTBD) и как её закрыть БЕЗ этого признака?
- В какую категорию мы перейдём?
- Кто будет потребителем после переопределения?
- Какая новая сущность приходит на смену?

Сгенерируй 4-6 карточек. Не больше — это глубокая работа, лучше 4 сильных reframe-а чем 10 поверхностных.

ПРАВИЛА:
- Каждый reframe — это **другая сущность**, не вариант. Если ты можешь сказать "это всё ещё стол" — значит ты не дошёл до reframe-а, вернись к 2.1.
- Связь с JTBD обязательна. Reframe который не закрывает изначальную job — пустая фантазия.
- Приводи реальные аналоги если есть (как iPad / Tesla / Uber в примерах выше).
- Не путай reframe с трансформацией. Трансформация: "стол на одной ножке". Reframe: "не нужен стол — еда вообще не на столе" (meal kits → еда едется руками; stand-up dining; dashboard холдер в машине).
- Title формат: "Reframe: <от чего> → <к чему>"
- Маркер уверенности для каждого пункта внутри карточки обязателен ([факт] / [типовой паттерн] / [предположение] / [нужно проверить])
- derived_from: ID карточек из 1.4 (именно Obligatory Features) + 1.5 (JTBD)

${FMT_RULES}`,

  // ═════ Остальные этапы (Module 03-04) ═════

  friction: (ctx, prev, existing) => `${ctx}
${prev ? `\nКонтекст предыдущих этапов:\n${prev}` : ''}
${existingBlock(existing)}
Stage: Friction Map
Identify the most painful friction points in this topic. Be specific: WHO experiences it, WHEN, and what exactly breaks down. Focus on moments where time, money, or energy is wasted or where people give up.
derived_from: ID Failure Modes из 1.7 и других релевантных карточек.

${FMT_RULES}`,

  contradiction: (ctx, prev, existing) => `${ctx}
${prev ? `\nКонтекст предыдущих этапов:\n${prev}` : ''}
${existingBlock(existing)}
Stage: Contradiction Finder
Surface the core tensions in this space. Format: "X requires Y but also requires NOT-Y." These contradictions are where breakthroughs live.
derived_from: ID точек трения и измерений из Fundamental Dimensions.

${FMT_RULES}`,

  cross_field: (ctx, prev, existing) => `${ctx}
${prev ? `\nКонтекст предыдущих этапов:\n${prev}` : ''}
${existingBlock(existing)}
Stage: Cross-field Transfer
For each contradiction above, find a different industry or business sector that faced a structurally identical tension and solved it.

HARD RULES:
- Priority: look in OTHER BUSINESS SECTORS first — retail, logistics, insurance, banking, pharma, real estate, franchising, B2B SaaS, media, staffing, marketplaces, manufacturing, telecom, etc.
- BANNED (too abstract, not useful for business): biology, military logistics, game design, urban planning, cooking, architecture, medieval history, nature metaphors. Reject these immediately.
- Each card MUST use a different industry. No repeats.

Title format: "<Industry>: <principle>"
derived_from: ID противоречий, для которых найдена аналогия.

${FMT_RULES}`,

  opportunity: (ctx, prev, existing) => `${ctx}
${prev ? `\nКонтекст предыдущих этапов:\n${prev}` : ''}
${existingBlock(existing)}
Stage: Opportunity Tree
Synthesize the insights so far into concrete opportunity spaces. For each: name the beneficiary, the unmet need, and what makes it now possible. Not hypotheses yet — just directions worth exploring.
derived_from: ID аналогов, противоречий, точек трения, JTBD.

${FMT_RULES}`,

  hypothesis: (ctx, prev, existing) => `${ctx}
${prev ? `\nКонтекст предыдущих этапов:\n${prev}` : ''}
${existingBlock(existing)}
Stage: Hypothesis Generation
Turn the selected opportunities into testable business hypotheses. Format: "We believe [specific customer] will [specific action] because [insight], resulting in [business outcome]." Each must be falsifiable.
derived_from: ID opportunity spaces.

${FMT_RULES_METRICS}`,

  critic: (ctx, prev, existing) => `${ctx}
${prev ? `\nГипотезы для критики:\n${prev}` : ''}
${existingBlock(existing)}
Stage: Critic Pass
For each hypothesis above, be its harshest critic. What are the 3 most likely failure modes? What assumptions must hold true? Be ruthlessly honest.
Title format: "Критика: <hypothesis name>"
derived_from: ID гипотезы которую критикуем.

${FMT_RULES_METRICS}`,

  shortlist: (ctx, prev, existing) => `${ctx}
${prev ? `\nГипотезы + критика:\n${prev}` : ''}
${existingBlock(existing)}
Stage: Shortlist
${existing
  ? 'The top hypotheses are already shortlisted. Now surface the NEXT TIER — "dark horse" candidates with significant weakness. Continue numbering after the last already-generated rank.'
  : 'From the hypotheses and critiques above, select the 3-5 that best survived scrutiny. Rank them #1 being strongest.'}
Title format: "#<rank>: <hypothesis name>"
derived_from: ID гипотезы и её критики.

${FMT_RULES_METRICS}`,

  validation: (ctx, prev, existing) => `${ctx}
${prev ? `\nШортлист:\n${prev}` : ''}
${existingBlock(existing)}
Stage: Validation Plan
For each shortlisted hypothesis, design the cheapest and fastest test that could prove or disprove it. Specify: what to test, how, what metric = true, what metric = false, rough time+cost estimate.
Title format: "Тест: <what>"
derived_from: ID гипотезы из шортлиста.

${FMT_RULES_METRICS}`,
};

export function buildPrompt(
  stageId: StageId,
  project: Project,
  prevCards: Card[],
  existingCards: Card[] = []
): string {
  const ctx = formatContext(project);
  const prev = formatCards(prevCards);
  const existing = formatCards(existingCards);
  return STAGE_PROMPTS[stageId](ctx, prev, existing);
}
